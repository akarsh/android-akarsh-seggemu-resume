<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Downloading the JSON file from the internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Checking the internet connection -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Reading a file from the external storage -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <!-- Creating a file and folder in the external storage -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:resizeableActivity="false"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".ResumeSchemaActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
        <activity android:name=".ContactActivity" />
        <activity android:name=".InfoActivity" />
        <activity android:name=".SummaryActivity" />
        <activity android:name=".ProfilesActivity" />
        <activity android:name=".SkillsActivity" />
        <activity android:name=".LanguagesActivity" />
        <activity android:name=".EducationActivity" />
        <activity android:name=".ExperienceActivity" />
        <activity android:name=".VolunteerActivity" />
        <activity android:name=".AwardsActivity" />
        <activity android:name=".PublicationsActivity" />
        <activity android:name=".InterestsActivity" />
        <activity android:name=".ReferencesActivity"></activity>
    </application>

</manifest>